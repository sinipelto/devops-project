{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft": "Warning",
      "Microsoft.Hosting.Lifetime": "Information"
    }
  },
  "HttpServ": {
    "Uri": "http://localhost:6000"
  },
  "DockerApi": {
    "MainContainerName": "apigateway", // To separate which container contains code for controlling the other containers
    "SocketPath": "unix:///var/run/docker.sock"
  }
}
